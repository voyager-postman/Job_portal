import { useTranslation } from "react-i18next";

function JobSearchMain() {
  const { t } = useTranslation("global");

  return (
    <div className="available-job-posts-info">
      {/* all job cards shown in /job-search */}
    </div>
  );
}

export default JobSearchMain;
